spring:
  datasource:
    url: jdbc:h2:mem:devdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    hibernate:
      # create-drop permite que Hibernate cree el esquema en H2 sin Flyway
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
  
  h2:
    console:
      enabled: true
      path: /h2-console
  
  # Flyway desactivado en DEV: H2 no es compatible con SQL PostgreSQL (SERIAL, etc.)
  # Usa ddl-auto: create-drop para generar el esquema automáticamente desde las entidades.
  flyway:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: "health,info,metrics"
  endpoint:
    health:
      show-details: always

info:
  app:
    name: "NexooHub Almacén - DEV"
    description: "Microservicio de gestión de inventarios para refaccionarias (Desarrollo con H2)"
    version: "1.0.0-SNAPSHOT"
    environment: "development"

logging:
  level:
    root: INFO
    com.nexoohub.almacen: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

jwt:
  secret: ${JWT_SECRET:NexooHubDevSecretKeyParaFirmaDeTokens2026!}
  expiration: 28800000  # 8 horas para desarrollo
