openapi: 3.0.0
info:
  title: API de Compatibilidad de Productos
  description: API para la gestión de la compatibilidad entre productos y motos.
  version: 1.0.0
servers:
  - url: /api/v1
paths:
  /compatibilidad:
    post:
      summary: Enlazar un producto con una moto
      description: Registra la compatibilidad entre un producto (pieza) y una moto.
      operationId: enlazarProductoConMoto
      tags:
        - Catálogo
      requestBody:
        description: Datos de la compatibilidad a registrar.
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompatibilidadProductoInput'
      responses:
        '201':
          description: Compatibilidad registrada correctamente.
          content:
            application/json:
              schema:
                type: object
                properties:
                  exitoso:
                    type: boolean
                    example: true
                  mensaje:
                    type: string
                    example: Compatibilidad registrada correctamente
                  id:
                    type: integer

  /compatibilidad/producto/{sku}:
    get:
      summary: Buscar compatibilidad por SKU
      description: Devuelve una lista de motos compatibles con un producto (pieza) específico, buscado por su SKU.
      operationId: buscarPorSku
      tags:
        - Catálogo
      parameters:
        - name: sku
          in: path
          description: SKU del producto a buscar.
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Lista de compatibilidades obtenida con éxito.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CompatibilidadProducto'

  /compatibilidad/moto/{motoId}:
    get:
      summary: Buscar compatibilidad por ID de Moto
      description: Devuelve una lista de productos (piezas) compatibles con una moto específica, buscada por su ID.
      operationId: buscarPorMoto
      tags:
        - Catálogo
      parameters:
        - name: motoId
          in: path
          description: ID de la moto a buscar.
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Lista de compatibilidades obtenida con éxito.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CompatibilidadProducto'

components:
  schemas:
    CompatibilidadProducto:
      type: object
      properties:
        id:
          type: integer
        skuInterno:
          type: string
        motoId:
          type: integer

    CompatibilidadProductoInput:
      type: object
      properties:
        skuInterno:
          type: string
        motoId:
          type: integer
      required:
        - skuInterno
        - motoId
